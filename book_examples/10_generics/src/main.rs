// Generics are abstract stand-ins for concrete types or other properties
// We can express the behaviour of generics without knowing what they will be

mod generics;
mod traits1;
mod traits2;

fn main() {
    // generics::examples();
    // traits1::examples();
    traits2::examples();
}
